export namespace CanvasType {
	// Specify width and height.
	export type Dimensions = {
		width: number,
		height: number
	};

	// Specify x and y.
	export type Coordinates = {
		x: number;
		y: number;
	}
}
